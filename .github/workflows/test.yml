name: Test Action

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use custom action
        uses: ./
        with:
          example-input: "Hello"

      - name: Assert expected output
        run: |
          echo "Tests passed"
